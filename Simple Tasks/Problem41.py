import math
angle_rad = float(input("Enter the angle in radians: "))

cosine_value = math.cos(angle_rad)
sine_value = math.sin(angle_rad)

print(f"Cosine of {angle_rad:.2f} radians is {cosine_value:.2f}")
print(f"Sine of {angle_rad:.2f} radians is {sine_value:.2f}")
